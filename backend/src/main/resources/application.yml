spring:
  application:
    name: truthlens-backend
  config:
    import: "configserver:http://truthlens-config-server:8888"

eureka:
  client:
    serviceUrl:
      defaultZone: http://truthlens-discovery-server:8761/eureka/