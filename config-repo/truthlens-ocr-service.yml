spring:
  threads:
    virtual:
      enabled: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 0

gemini:
  api:
    key: ${GEMINI_API_KEY}

eureka:
  client:
    serviceUrl:
      defaultZone: http://truthlens-discovery-server:8761/eureka/
